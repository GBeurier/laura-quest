Use your built-in image_gen tool (the "imagegen" skill) to GENERATE raster pixel art. Do NOT use the CLI fallback (no OPENAI_API_KEY). Do NOT draw procedurally (no PIL/canvas/SVG). The artwork MUST come from your AI image generator.

The ATTACHED image is a finished pixel-art SPRITE SHEET (6 frames in one horizontal row, flat transparent background) of a small ENEMY for a lighthearted cartoon platformer: an angry anthropomorphic BEIGE CARDBOARD DOCUMENT FOLDER ("rapport ADEME") wearing a blue French beret and black glasses, with a round red angry rubber-stamp face on its cover, a blue-white-red bookmark, thin arms and legs, holding a bundle of letters. Frames 1-3 = walk/idle loop, frame 4 = hurt pose, frames 5-6 = attack (it throws a rolled letter, papers flying).

YOUR ONE JOB: redraw this EXACT sheet but add the OFFICIAL ADEME BRANDING on the folder's front cover, clearly readable in every frame where the cover is visible:
- the capital letters "ADEME" written HORIZONTALLY in DARK BLUE chunky pixel letters (thick, readable at small size) across the lower half of the cover, below the red stamp face;
- a small bright GREEN LEAF/sprout accent right next to or above the lettering (eco-agency look).
Keep the red angry stamp face, the beret, the glasses, the bookmark and the letters bundle as they are.

FAITHFULNESS IS EVERYTHING — copy the input as closely as you can: SAME number of frames (6), SAME poses in the SAME order, SAME body proportions, SAME size, SAME scale and SAME position of the character in each cell, SAME palette and outline style. Change ONLY the front cover (add the ADEME lettering + green leaf). The character FACES RIGHT, as in the reference.

PIXEL-ART STYLE: retro 16-bit SNES / arcade run-and-gun look. Crisp visible square pixels, bold dark outline, limited vibrant palette, flat cel shading, chunky READABLE shapes. NO smooth gradients, NO anti-aliasing, NO airbrushed/painterly look, NO 3D.

LAYOUT (critical): EXACTLY 6 frames in a SINGLE horizontal row, on a uniform grid of 6 equal cells. Same cell size for all; the character at the SAME scale, standing on the SAME ground baseline near the bottom of each cell, centered in its cell. Leave a WIDE empty magenta gap between consecutive frames (each frame a clearly separated island; frames MUST NOT touch or overlap; nothing crosses into a neighbour).

BACKGROUND: fill the ENTIRE image (including the gaps) with ONE perfectly FLAT solid #ff00ff magenta. NO shadow, NO contact shadow, NO floor line, NO gradient, NO grid lines, NO frame numbers, NO text outside the ADEME lettering on the cover, NO watermark. NEVER use magenta or pink anywhere on the character.

After image_gen returns, copy ONLY the PNG created by THIS Codex session to:
  /home/delete/laura_quest/_monstgen/raw/ademe_logo_sheet.png
CRITICAL: do NOT pick "the newest PNG" globally from ~/.codex/generated_images — use only the generated_images subdirectory for YOUR OWN Codex session id (printed in your header). If you cannot unambiguously identify your own PNG, copy nothing and print FAIL_UNSAFE_COPY.

Generate the sheet (ONE image_gen call), then cp it. Do NOT run chroma-key removal, do NOT delete any files. Finally print exactly:
  SHEET: /home/delete/laura_quest/_monstgen/raw/ademe_logo_sheet.png (<W>x<H>)
