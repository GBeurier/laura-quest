Use your built-in image_gen tool (the "imagegen" skill) to GENERATE raster pixel art. Do NOT use the CLI fallback (no OPENAI_API_KEY). Do NOT draw procedurally (no PIL/SVG/canvas). The artwork MUST come from your AI image generator. You will generate three separate game-item icons (one image_gen call each).

STYLE (every icon): retro 16-bit pixel-art GAME ICON, classic LucasArts "Monkey Island" / SCUMM vibe. Crisp visible square pixels, bold dark brown outline, limited warm vibrant palette, flat cel shading, chunky readable CUTE shapes, a touch of humor. NO smooth gradients, NO anti-aliasing, NO 3D, NO painterly/airbrushed look.

COMPOSITION (every icon): ONE single object, centered, SMALL, with WIDE empty padding all around (the object fills about 55-65% of the square frame and never touches the edges). These belong to a cheerful agrivoltaics-PhD farming platformer.

BACKGROUND (every icon): fill the ENTIRE image with ONE perfectly FLAT solid #ff00ff magenta. NO shadow, NO contact shadow, NO floor line, NO gradient, NO vignette, NO text, NO frame, NO grid, NO watermark. NEVER use magenta or pink anywhere on the object itself.

IMAGE 1 -> a cozy inviting cup of warm latte coffee in a rounded mug, with a cute little HEART drawn in the cream foam and gentle curls of steam, cheerful comforting and appetizing, a delightful healing pick-me-up. ABSOLUTELY NO skull, NO crossbones, nothing scary, dark or toxic — it must look POSITIVE and tasty. cp the generated PNG to /home/delete/laura_quest/_icongen/raw/pickup_cafe.png

IMAGE 2 -> a small pile of plump ROUND garden seeds (pumpkin-seed and round bean shapes, warm brown/tan) with one little green sprout popping up, clearly gardening seeds about to grow. NOT rice, NOT cereal grains. cp the generated PNG to /home/delete/laura_quest/_icongen/raw/pickup_graine.png

IMAGE 3 -> a compact little cluster of three plump ROUND brown garden seeds (pumpkin-seed teardrop shape with a visible seam), clearly SEEDS, a roundish compact group. Definitely NOT a pale elongated rice grain and NOT cereal. cp the generated PNG to /home/delete/laura_quest/_icongen/raw/ammo_graine.png

Generate the images ONE AT A TIME (one image_gen call, then cp, then the next). Do NOT run chroma-key removal, do NOT delete any files. When all are done, print one line per image: '<name>: <path> (<W>x<H>)'.
