Use your built-in image_gen tool (the "imagegen" skill) to GENERATE one raster image. Do NOT use the CLI fallback (no OPENAI_API_KEY). Do NOT draw procedurally (no PIL/canvas/SVG). The artwork MUST come from your AI image generator.

Generate ONE horizontal PIXEL-ART SPRITE SHEET.

PIXEL-ART STYLE: retro 16-bit SNES / arcade run-and-gun look. Crisp visible square pixels, bold dark outline, limited vibrant palette, flat cel shading, chunky readable shapes. NO smooth gradients, NO anti-aliasing, NO airbrushed/painterly rendering, NO 3D.

LAYOUT (critical): EXACTLY 6 frames in a SINGLE horizontal row, left to right, on a uniform evenly-spaced grid of 6 equal cells. Every frame the SAME cell size; the character drawn at the SAME scale and standing on the SAME ground baseline near the bottom of each cell; centered in its cell; leave a little EMPTY background padding between frames so adjacent frames never touch or overlap. The character's identity, outfit and colors must be IDENTICAL in all 6 frames — only the POSE changes frame to frame (this is one animation).

CHARACTER (identical in every frame): a tall, big, OLD, BALD male professor; mean evil glare; short 3-day WHITE stubble beard; open WHITE lab coat over a shirt with a blue tie; round glasses; dark trousers; brown shoes. A comedic villain boss.

ANIMATION (6 frames, character FACING LEFT): an ATTACK throw cycle -> f1 standing guard, f2 wind up leaning back, f3 raise a rolled-up paper chart, f4 thrust arm forward to the LEFT releasing the chart, f5 follow-through lunge, f6 settle back. Smooth readable loop.

BACKGROUND: fill the ENTIRE image (including the gaps between frames) with ONE perfectly FLAT solid #ff00ff magenta. NO shadows, NO floor line, NO gradient, NO grid lines drawn, NO frame numbers, NO text, NO watermark. Never use magenta/pink on the character.

After generating, cp the final PNG to /home/delete/laura_quest/_bossgen/raw/michael_atk_sheet.png and print its path and WxH.
